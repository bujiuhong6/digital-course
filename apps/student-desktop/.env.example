# 教学 API 根地址（Vite 注入，见 `src/api.ts`）
# 开发（import.meta.env.DEV）时默认**空基址 + Vite 代理** `/v1` → 127.0.0.1:8000，减少 Tauri WebView「Load failed」。
# 若仍要直连 API，可设 VITE_DEV_API_PROXY=0 并保留下方地址。
# VITE_DEV_API_PROXY=0
# VITE_API_BASE_URL=http://127.0.0.1:8000

# Pyodide 包目录（须指向 .../full/，以 / 结尾）；默认与 index.html 中 jsdelivr 版一致
# VITE_PYODIDE_INDEX_URL=https://cdn.jsdelivr.net/pyodide/v0.27.0/full/
# 可选：pyodide.js 脚本地址（与上版本一致；国内无法访问 cdn 时自托管并填此项）
# VITE_PYODIDE_SCRIPT_URL=https://cdn.jsdelivr.net/pyodide/v0.27.0/full/pyodide.js
