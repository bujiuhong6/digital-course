# AI + Python 教学系统 — 准备清单（续篇：分阶段、MVP、风险、待澄清）

**前提**：在 `vendor/superpowers/skills/brainstorming/SKILL.md` 工作流下，大系统已拆为子域。本文接续「整体准备清单」，给出**落地顺序、一期范围建议、风险与需拍板问题**，供进入 `writing-plans` 前复查。

**关联**：首版十段清单已覆盖 产品与教学、架构分叉、身份与 OAuth、内容作业、Jupyter 安全、LLM、工程基础设施、合规、团队、关键分叉。此处不再逐条重复，只补**执行层**与**分期**。

---

## 1. 建议的交付阶段（从准备到可运营）

| 阶段 | 目标 | 依赖准备（摘要） |
|------|------|------------------|
| M0 概念验证 | 浏览器内能跑通「登录 mock + 一个静态章 + 一个内嵌执行单元（哪怕是简化 REPL 或单细胞 Notebook）+ 调一次 LLM」 | 云账号、一个域名、模型 API 试用水位、Jupyter/替身的选型 spike |
| M1 教学端 MVP | 章节点播、**作业提交**（可先文件上传+存储）、学生列表级作业状态、**教师查看提交与简单打分** | 数据库模式、基础 RBAC、对象存储、最少监控 |
| M2 执行与体验 | **完整 Jupyter/Notebook 多租户**、资源限额、防滥用、作业与章节强绑定、站内通知 | 容器编排或托管 Notebook、安全评审、成本模型 |
| M3 身份与增长 | **账号密码 + 微信/QQ 登录**、备案与 OAuth 全链路、**隐私与条款**上站 | 备案完成、各开放平台应用审核、法律文本 |
| M4 智能辅导 | **Chatbot** 全站与题目上下文、可选 RAG 接课程知识、审计与防注入 | 向量库与分块、提示与护栏、对话留存策略 |

**分期原则**：你要求里「无本地环境、内嵌 Notebook、老师批改、OAuth」全部硬塞进一期会拉长上线并抬高合规成本；**常见做法**是 M1 先上教学+作业+账号密码+邮件，M3 再上微信/QQ 与**境内要求**强绑定的能力。

---

## 2. MVP 与「全量」的边界（避免范围泄漏）

- **MVP 可包含**：学生/教师角色、章与作业 CRUD、提交物存储、**教师打分+评语**、**嵌入一种受控的代码运行或 Notebook 路径（先一种即可）**、**最简 LLM 答疑（有免责声明与限流）**。
- **常后置**：QQ/微信二选一同时上、RAG 全库、自动判题、直播、复杂学情分析。
- **与「无额外环境」强相关**：M0 就要验证**执行服务**的延迟与包管理策略（预装镜像 vs 动态安装），避免后期推翻 Notebook 方案。

---

## 3. 技术选型准备（在清单之外需完成的「spike」小实验）

- **Notebook**：在目标云上跑通一次 **JupyterHub / Binder 类**小集群，或商业 Notebook API 的 iframe 集成，记录：冷启动时间、并发热度、**单用户成本**。
- **LLM 网关**：统一流式/非流、计费、**境内 endpoint** 是否可用、对话日志脱敏样例跑一遍。
- **OAuth 回调**：在**已备案的测试子域**上跑通一个社交登录 sandbox（可先用一种）。

---

## 4. 风险登记（准备期就要有人盯）

| 风险 | 说明 | 缓解 |
|------|------|------|
| 备案与 OAuth 时序 | 微信/QQ 网站应用要合法域名与备案材料 | 并行准备主体资质；开发期用密码登录 |
| 算力与 LLM 费用 | Notebook + 大模型**双高成本** | 限并发、包预装、模型分级（答疑用小模型试跑） |
| 安全与滥用 | 用户代码执行 = 高攻击面 | 网络隔离、超时、只读基镜像、全量审计 |
| 产品合规 | 教育数据、未成年人、AI 生成内容 | 条款、年龄门、老师可见范围设计 |

---

## 5. 待澄清问题（拍板后写正式设计规格与 writing-plans）

**下列每项选一项，即可把架构图定死：**

1. **用户地域**：主战场是否**仅中国大陆**？（影响备案、支付、可接入的 LLM 与微信/QQ 产品形态。）
2. **Notebook 一期**：M1 是「内嵌**完整** Jupyter」还是 **「REPL/轻量 cells + 二期 Jupyter」**？
3. **微信与 QQ** 是否必须**同一发布火车**上齐？若只能先上一个，**优先微信还是 QQ**（与目标学生人群有关）？
4. **Chatbot**：练习页是否允许把**学生整段作业代码**发模型？**教师是否默认**可见对话？（强合规场景常默认「可见」或「可开关」。）

---

## 6. 下一步（Superpowers 流程衔接）

- 你对 **第 5 节**四个问题有答复后，可将答复写入本节下方「决策记录」表，并产出「正式版」`YYYY-MM-DD-ai-python-teaching-system-design.md`（架构、数据流、子系统接口）。
- 设计经你确认后，再按 `vendor/superpowers/skills/writing-plans/SKILL.md` 拆**实现计划**（按文件路径、验证步骤、小任务 2–5 分钟级）。

### 决策记录（团队填写）

| 问题 | 决定 | 日期 |
|------|------|------|
| 1 地域与合规 |  |  |
| 2 Notebook 一期范围 |  |  |
| 3 OAuth 上线顺序 |  |  |
| 4 Chatbot 数据与可见性 |  |  |

---

*文档角色：准备清单续篇 + 分期与待澄清，不是最终实现规格。*
